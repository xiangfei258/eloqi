# Eloqui 设计蓝图

> 目标：从零重写一个跨平台桌面语音输入工具，MIT 许可证，完全原创代码。
> 本文档只描述功能规格、架构思想、平台技术选型与设计取舍，是“思想层”的蓝本，
> 不包含任何可照抄的代码实现。实现时请用你自己的表达重新编写。

---

## 1. 产品定位

一个常驻后台的桌面语音输入工具。用户按下全局快捷键开始说话，语音被实时转成文字，
然后自动写入剪贴板，或直接“上屏”（模拟按键粘贴）到当前聚焦的输入框。

核心价值：减少键盘输入，适合写代码、聊天、记笔记、输入长文本。

目标平台：Linux（Wayland + X11）、macOS、Windows，三平台全支持。

---

## 2. 核心功能规格

### 2.1 全局热键（核心交互）
- 支持两种触发模式：
  - toggle：按一下开始录音，再按一下停止。
  - hold：按住说话，松开停止。
- 热键必须限定为适合做全局快捷键的按键，避免和日常打字冲突：
  - 允许：纯修饰键组合（如 Alt+Super、Ctrl+Alt）、功能键（F1–F24）、Tab、CapsLock、
    方向键、Home/End/PageUp/PageDown、Insert/Delete、小键盘数字键。
  - 不允许：字母、数字、标点、空格等普通字符键。
- 修饰键组合要求精确匹配：例如 Alt+Super 意味着按下这两个键时触发，按 Alt+Super+Tab
  不应误触发（要保证不吞掉 Alt+Tab 这类系统组合键）。
- 录音/停止/出错期间，临时注册辅助热键：
  - 停止或收尾时：Esc 取消当前录音。
  - 出错状态时：R 重试上一次操作。

### 2.2 语音识别（ASR）引擎
- 设计为可插拔接口，至少支持两类：
  - 流式：边录边识别，实时返回中间结果，最终返回最终结果。
  - 非流式：录完后一次性上传完整音频，返回最终文本（适合 OpenAI 兼容的
    Whisper 接口、Groq、本地 whisper.cpp / 本地兼容服务）。
- 接口抽象应统一两类实现的差异，上层只关心：连接、发送音频、结果回调、最终文本、关闭。
- 支持热词增强：让 ASR 优先识别项目名、人名、英文术语、专有名词。
- 支持可配置的语言（如 zh-CN、en-US）。

### 2.3 输出（剪贴板 / 自动上屏）
- 识别出最终文本后，二选一：
  - 复制到剪贴板：用户手动粘贴。
  - 自动上屏：直接模拟粘贴动作把文本注入到当前焦点窗口。
- 自动上屏在 Linux 上通常是“写入剪贴板 + 模拟粘贴快捷键”，要处理不同桌面环境的差异。
- 防重复输出：一次录音会话的最终文本只输出一次（流式结果、最终回调、关闭回调
  等多条路径都可能到达，需要去重）。

### 2.4 录音状态提示（overlay / 胶囊）
- 录音时在屏幕上显示一个小的状态胶囊，提示当前状态。
- 状态至少覆盖：连接中、录音中、停止缓冲中、等待结果、出错。
- 不同平台用不同技术实现悬浮提示（见第 4 节平台矩阵）。

### 2.5 TUI 配置界面
- 一个终端配置界面，可修改：热键、模式、ASR 引擎及密钥、自动上屏、停止延迟、热词、语言。
- 配置改动实时热重载（监听配置文件变化）。
- 正常日志不能污染 TUI 界面；调试日志仅在 debug 时显示。

### 2.6 录音历史统计
- 统计：录音次数、总字数、总录音时长、最近一次字数和时长。
- 持久化到本地状态文件。

### 2.7 启动环境检查（doctor）
- 启动时检查运行环境，缺失依赖时给出简短、可操作的修复提示。
- 检查项应面向用户可执行的动作（例如“安装 wl-clipboard”“把用户加入 input 组”），
  不要罗列用户无法行动的底层实现细节。

---

## 3. 架构设计（分层）

采用“一个核心引擎 + 可插拔插件 + 平台能力层”的分层：

    入口 (main)
      ├─ 配置加载 (config)
      ├─ 环境检查 (doctor)
      ├─ 热键 Provider 工厂（按平台选择）
      ├─ 核心引擎 (engine)：插件生命周期 + 配置热重载
      ├─ 插件 (plugins)：
      │    ├─ voice：录音、ASR、热键行为、剪贴板/上屏分发、统计
      │    └─ overlay：录音状态胶囊
      └─ 平台能力层 (platform / internal)：
           ├─ hotkey    全局热键
           ├─ recorder  音频采集
           ├─ clipboard 剪贴板
           ├─ autotype  自动上屏
           └─ overlay   悬浮提示

分层原则：
1. 上层（插件）只依赖接口，不依赖具体平台实现。
2. 平台差异用 build tags（go:build linux/darwin/windows）隔离到独立文件。
3. 每个平台能力都应有 接口 + 工厂 + mock 实现，便于跨平台测试核心逻辑。
4. 插件注册热键必须发生在初始化阶段，而非启动阶段，避免事件分发延迟导致丢事件。

---

## 4. 平台能力矩阵（技术选型，重写的关键）

这是本项目最难、最需要提前规划的部分。每格是该能力在该平台上的实现方案。

| 能力 | Linux Wayland | Linux X11 | macOS | Windows |
|---|---|---|---|---|
| 全局热键 | evdev 直接读 /dev/input/event*（需 input 组权限） | 原生 X11（XGrabKey / 事件循环） | CGEventTap（需辅助功能权限） | GetAsyncKeyState 轮询（约 5ms）+ WH_KEYBOARD_LL 低级钩子做物理边沿回退 |
| 录音 | 调外部 arecord（ALSA，16kHz/16bit/单声道 raw PCM） | 同左 | CoreAudio / AudioQueue | WinMM waveIn（16kHz/16bit/mono PCM） |
| 剪贴板 | wl-copy / wl-paste | xclip（含 primary selection） | NSPasteboard | Win32 Unicode 剪贴板 |
| 自动上屏 | wtype，或 /dev/uinput（KDE Plasma 场景）；写剪贴板后模拟 Ctrl+V | XTest 模拟按键（如 Shift+Insert） | 原生键盘事件注入 | SendInput 发送 Ctrl+V |
| 状态胶囊 | wlr-layer-shell（Sway/wlroots）；GNOME 下回退到 notify-send | 原生 X11 无边框置顶窗口 | AppKit NSPanel（辅助进程） | 无激活、置顶、点击穿透的 Win32 窗口 |

平台关键注意点（踩坑经验，属于可复用的技术知识）：

- Wayland：
  - 热键用 evdev 时不要抢占式 grab 修饰键（EVIOCGRAB），否则会吞掉 Alt+Tab 等系统组合。
  - 需要遍历所有 /dev/input/event* 找到键盘设备。
  - GNOME/Mutter 不支持 wlr-layer-shell，overlay 必须回退到桌面通知。
- X11：
  - 自动上屏涉及 primary selection（中键粘贴）与 clipboard 的区别。
- macOS：
  - 权限授予给“启动程序的那个终端”，不是 .app 包。
  - overlay 用独立的 NSPanel helper 进程，主程序通过 stdin 向它发命令。
- Windows：
  - 低级钩子必须是观察式的：事件一律传给下一个钩子，不消费、不重放修饰键，
    保证 Alt+Tab 等系统快捷键不受影响。
  - 修饰键组合要求精确修饰键集合，避免误触发。
  - 无需外部工具（ffmpeg/SoX）、无需 cgo、无需管理员权限。

---

## 5. 关键设计决策（重写时重点复刻的“思想”）

### 5.1 录音状态机
用显式状态机管理一次录音会话，状态至少包括：

    idle → connecting → recording → stopping_delayed → stopping → (回到 idle)
                                  ↘ error → (超时或 R 重试) → idle

- idle：待机，等待热键。
- connecting：录音已开始，但 ASR 连接尚未建立。
- recording：正在录音且 ASR 已连接。
- stopping_delayed：已触发停止，但在“停止延迟缓冲”内（等话尾）。
- stopping：停止录音、发送最后音频、等待最终结果。
- error：出错，保持一段时间展示错误信息，可重试。

为什么用显式状态机：快速连按热键、hold 模式快速松开、错误重试等场景是历史易错点，
用“状态 + 合法转移表 + 单测覆盖全部转移”可以锁死这些边界。

### 5.2 停止与收尾分离（异步化）
- 热键处理要快：热键回调里只更新状态、标记，不做重活。
- 停止是后台工作：停止录音、发送最后一段音频、等待 ASR 最终结果、写剪贴板、
  自动上屏，这些全部放到后台 goroutine 完成，避免阻塞热键事件循环。
- 停止动作可以分解成清晰的阶段（停止录音器 → 发最终音频 → 等最终结果 → 关闭客户端），
  每个阶段都应能定位卡在哪一步（便于调试）。

### 5.3 停止延迟缓冲
- 松开热键后，不立即停止，而是等一个可配置的延迟（默认约 800ms），
  用于补录话尾；期间再次按下可取消停止、继续录音。
- 这是提升识别完整性的关键体验设计。

### 5.4 双输出防重
- 一次会话的最终文本只输出一次，用会话 ID 去重。
- 因为“最终结果回调”和“关闭回调”等多条路径都可能到达，必须在输出前做原子化的“认领”
  操作，谁先认领谁输出，后续路径不再重复。

### 5.5 事件队列
- 热键事件进入带缓冲的队列，由独立 goroutine 消费，避免快速连按丢失事件。

### 5.6 配置热重载
- 监听配置文件变化，变化后重新加载并通知各插件。
- 监听目录而非文件本身（文件被 os.Create 截断重写时 inode 会变，监听文件会失效）。

---

## 6. 数据流（一次录音的完整路径）

    用户按下热键
      → 热键 Provider 发出事件（边沿：按下/抬起，不重复）
      → 事件队列 → voice 插件处理
      → 启动录音器（16kHz/16bit/mono PCM）
      → 建立 ASR 连接（connecting）
      → 流式读取音频 → 发送给 ASR（recording，实时中间结果）
      → 用户停止（松开/再按）
      → 停止延迟缓冲（stopping_delayed）
      → 停止录音器、发送最后音频（stopping）
      → 等待 ASR 最终结果
      → 去重后输出：写剪贴板 或 自动上屏
      → 回到待机（idle）

---

## 7. 技术选型建议

- 语言：Go。理由：单二进制分发、交叉编译、启动快、内存低、
  goroutine 天然契合“热键事件循环 + 音频流式上传 + 结果回调 + 定时刷新”的多路并发。
- 配置：TOML 或 YAML。
- 日志：结构化日志（如 Go 的 slog），TUI 模式下日志进文件、不污染终端。
- TUI：Bubble Tea（Go 生态成熟的终端 UI 库）。
- 测试：核心逻辑用 table-driven tests + mock 平台能力 + go test -race。

---

## 8. 分阶段实现路线图

| 阶段 | 内容 | 目标 |
|---|---|---|
| P0 | 定名、仓库初始化、MIT 许可证、项目骨架 | 干净的起点 |
| P1 | 平台能力层接口 + 单平台打通（建议先 Linux） | 跑通“录音→识别→输出”最小闭环 |
| P2 | 显式状态机 + 停止延迟 + 双输出防重 + 测试 | 核心逻辑稳、可测 |
| P3 | 补齐 macOS + Windows | 三平台全支持 |
| P4 | TUI 配置界面 + 配置热重载 + doctor | 产品化 |
| P5 | 统计、热词、错误重试、状态胶囊打磨 | 体验完善 |
| P6 | CI（三平台矩阵 + race + lint）、发布打包 | 工程化 |

---

## 9. 非功能需求

- 性能：热键响应要即时（事件处理不阻塞）；录音为低延迟流式。
- 可靠性：出错有明确提示和重试；单次会话结果不重复输出。
- 可维护性：平台差异隔离在 build-tag 文件；核心逻辑可脱离真实设备测试。
- 可移植：不依赖管理员权限；Linux 依赖的二进制（arecord/wl-clipboard/wtype/xclip）
  在 doctor 里检测并提示安装。
